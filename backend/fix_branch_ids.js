const mysql = require('mysql2/promise'); require('dotenv').config(); async function run() { const pool = mysql.createPool({ host: process.env.DB_HOST, user: process.env.DB_USER, password: process.env.DB_PASSWORD, database: process.env.DB_NAME }); const [res] = await pool.query('UPDATE students s JOIN course_branches cb ON s.branch = cb.name AND s.course_id = cb.course_id SET s.branch_id = cb.id WHERE (s.branch_id IS NULL OR s.branch_id = 0) AND s.branch IS NOT NULL AND s.branch != \'\''); console.log(res); process.exit(0); } run();
